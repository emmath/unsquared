<table>
  <tr>
    <th>Party Name</th>
    <th>Party Photo</th>
  </tr>

  <% @parties.each do |party| %>
    <tr>
      <td><%= link_to party.event_name, party %></td>
    </tr>
  <% end %>
</table>

<%= link_to "New Party", new_party_path %>
